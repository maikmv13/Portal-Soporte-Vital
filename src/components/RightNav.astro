---
import type { Props } from "@astrojs/starlight/props";
---

<nav class="right-nav">
  <div class="nav-links">
    <a href="/introduccion/presentacion" class="nav-link">
      <span>📖 Guía</span>
    </a>
    <a href="/trivial" class="nav-link nav-link--trivial">
      <span>🎯 Trivial</span>
    </a>
  </div>

  <div class="nav-divider"></div>

  <button
    class="nav-btn-primary"
    onclick="alert('Funcionalidad de inicio de sesión próximamente disponible.')"
  >
    <svg
      width="18"
      height="18"
      viewBox="0 0 24 24"
      fill="none"
      class="nav-btn-icon"
      ><path
        d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 4c1.93 0 3.5 1.57 3.5 3.5S13.93 13 12 13s-3.5-1.57-3.5-3.5S10.07 6 12 6zm0 14c-2.03 0-4.43-.82-6.14-2.88a9.947 9.947 0 0 1 12.28 0C16.43 19.18 14.03 20 12 20z"
        fill="currentColor"></path></svg
    >
    <span>Iniciar Sesión</span>
  </button>
</nav>

<style>
  .right-nav {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding-left: 1.5rem;
    border-left: 1px solid var(--sl-color-gray-5);
    margin-left: 1rem;
  }

  .nav-links {
    display: flex;
    gap: 0.25rem;
  }

  .nav-divider {
    width: 1px;
    height: 24px;
    background-color: var(--sl-color-gray-5);
    margin: 0 0.25rem;
  }

  .nav-link {
    text-decoration: none;
    color: var(--sl-color-gray-2);
    font-weight: 600;
    font-size: 0.95rem;
    padding: 0.45rem 1rem;
    border-radius: 99px;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    white-space: nowrap;
  }

  .nav-link:hover {
    color: #10b981;
    background: #f0fdf4;
  }

  .nav-link--trivial {
    color: #0f172a;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
  }

  .nav-link--trivial:hover {
    border-color: #34d399;
    color: #059669;
    background: #f0fdf4;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(16, 185, 129, 0.1);
  }

  .nav-btn-primary {
    cursor: pointer;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    background: #0f172a;
    color: white;
    font-family: inherit;
    font-weight: 700;
    font-size: 0.95rem;
    padding: 0.55rem 1.25rem;
    border-radius: 99px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    white-space: nowrap;
    border: 1px solid transparent;
  }

  .nav-btn-icon {
    opacity: 0.9;
  }

  .nav-btn-primary:hover {
    background: #1e293b;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px -6px rgba(15, 23, 42, 0.5);
  }

  @media (max-width: 50rem) {
    .right-nav {
      display: none;
    }
  }
</style>
