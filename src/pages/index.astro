---
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";
---

<StarlightPage frontmatter={{ title: "Inicio", template: "splash" }}>
  <style is:global>
    /* Nuke Starlight defaults for the Home */
    .main-frame {
      padding-top: 0 !important;
      padding-inline: 0 !important;
    }
    .sl-container {
      max-width: none !important;
      margin: 0 !important;
    }
    .content-panel {
      padding: 0 !important;
      border: none !important;
    }
    .sl-markdown-content {
      max-width: none !important;
    }
    /* Kill the "Inicio" title specifically */
    h1:not(.hero-title) {
      display: none !important;
    }
    /* Allow backgrounds to overflow */
    .main-content {
      overflow: visible !important;
    }
    .page-content {
      padding: 0 !important;
    }
    /* Force full width on the hero */
    .full-width-home {
      width: 100% !important;
      max-width: 100vw !important;
    }

    /* ======= BENTO LAYOUT (global to override Starlight) ======= */
    .bento-layout {
      display: grid !important;
      grid-template-columns: 1fr 1fr !important;
      gap: 1.25rem !important;
      align-items: stretch !important;
      width: 100% !important;
    }

    /* Critical: prevent Starlight from forcing 100% width on grid children */
    .bento-layout > * {
      width: auto !important;
      min-width: 0 !important;
      max-width: none !important;
    }

    .bento-subgrid {
      display: grid !important;
      grid-template-columns: 1fr 1fr !important;
      grid-template-rows: 1fr 1fr !important;
      gap: 1.25rem !important;
      width: auto !important;
      min-width: 0 !important;
    }

    /* Critical: prevent Starlight from forcing 100% width on subgrid children */
    .bento-subgrid > * {
      width: auto !important;
      min-width: 0 !important;
      max-width: none !important;
    }

    .bento-card {
      background: white !important;
      border-radius: 24px !important;
      border: 1px solid rgba(0, 0, 0, 0.05) !important;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.02) !important;
      overflow: hidden !important;
      display: flex !important;
      flex-direction: column !important;
    }

    .bento-card.featured {
      background: linear-gradient(135deg, #ffffff 0%, #f1f5f9 100%) !important;
      border: 1px solid rgba(16, 185, 129, 0.15) !important;
      padding: 3rem !important;
    }

    .bento-card.small {
      padding: 1.25rem !important;
    }

    .bento-card.small h4 {
      font-size: 1.1rem !important;
      font-weight: 800 !important;
      color: #1e293b !important;
      margin: 0.5rem 0 0.4rem !important;
    }

    .bento-card.small p {
      font-size: 0.9rem !important;
      line-height: 1.5 !important;
      color: #64748b !important;
      margin: 0 !important;
    }

    .bento-card.small .card-icon {
      font-size: 1.5rem !important;
      display: block !important;
    }

    .bento-card.accent {
      background: linear-gradient(135deg, #ecfdf5 0%, #f0fdf4 100%) !important;
      border: 1px solid #d1fae5 !important;
    }

    .bento-card.accent h4 {
      color: #059669 !important;
    }
    .bento-card.accent p {
      color: #065f46 !important;
    }
  </style>

  <div class="full-width-home">
    <section class="hero-section">
      <div class="hero-bg-glow"></div>
      <div class="hero-content">
        <div class="hero-text-side">
          <h1 class="hero-title">
            <span class="hero-top">🚑 Guía SVB y SVA</span>
            <span class="hero-main"
              >Soporte Vital Básico<br />y Avanzado 2025</span
            >
            <div class="hero-divider"></div>
          </h1>
          <p class="hero-tagline">
            👩‍⚕️ Por Aurora Vizoso García – Enfermera Asistencial FPUSG 061
          </p>
          <div class="hero-actions">
            <a href="/introduccion/presentacion" class="hero-btn-primary"
              >🎯 Comenzar el Curso</a
            >
          </div>
        </div>
        <div class="hero-image-side">
          <div class="hero-image-wrapper">
            <img src="/hero-cpr.png" alt="Soporte Vital" class="hero-cpr-img" />
            <div class="floater floater-heart">❤️</div>
            <div class="floater floater-ambulance">🚑</div>
            <div class="floater floater-erc">✓ ERC 2025</div>
            <div class="floater floater-saving">LIVE SAVING</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Wrapped in sl-markdown-content to allow styles to apply -->
    <div class="sl-markdown-content">
      <div class="content-sections">
        <!-- Bento Grid Features -->
        <section class="bento-section">
          <div
            class="bento-layout"
            style="display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; align-items: stretch; width: 100%;"
          >
            <!-- IZQUIERDA: Tarjeta destacada -->
            <div class="bento-card featured" style="min-width: 0;">
              <div class="card-glow"></div>
              <div class="card-content">
                <span class="card-icon-large">🎓</span>
                <div class="text-group">
                  <h4>Formación Integral de Élite</h4>
                  <p>
                    Un sistema educativo avanzado que cubre desde el
                    reconocimiento inicial de la parada hasta el manejo experto
                    de fármacos y vía aérea. Todo estructurado bajo las guías
                    oficiales 2025.
                  </p>
                  <ul class="card-list">
                    <li>✅ Algoritmos de decisión clínica</li>
                    <li>✅ Manejo fármaco-terapéutico</li>
                    <li>✅ Control avanzado de la vía aérea</li>
                  </ul>
                </div>
              </div>
            </div>
            <!-- DERECHA: Grid 2x2 -->
            <div
              class="bento-subgrid"
              style="display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; gap: 1.25rem; min-width: 0;"
            >
              <div class="bento-card small" style="min-width: 0;">
                <div class="card-content">
                  <span class="card-icon">🏥</span>
                  <h4>Inmersión Práctica</h4>
                  <p>
                    Simulaciones basadas en casos reales. Aprende a tomar
                    decisiones críticas bajo estrés.
                  </p>
                </div>
              </div>
              <div class="bento-card small" style="min-width: 0;">
                <div class="card-content">
                  <span class="card-icon">📜</span>
                  <h4>Registro CERCP</h4>
                  <p>
                    Contenido supervisado para garantizar la validez
                    institucional en todo el territorio español.
                  </p>
                </div>
              </div>
              <div class="bento-card small accent" style="min-width: 0;">
                <div class="card-content">
                  <span class="card-icon">🚀</span>
                  <h4>Tecnología 2025</h4>
                  <p>
                    Herramientas digitales avanzadas para visualización dinámica
                    de la fisiopatología cardíaca.
                  </p>
                </div>
              </div>
              <div class="bento-card small" style="min-width: 0;">
                <div class="card-content">
                  <span class="card-icon">🔍</span>
                  <h4>Evidencia Científica</h4>
                  <p>
                    Análisis de los últimos estudios del ERC e ILCOR. Medicina
                    basada en la evidencia actualizada.
                  </p>
                </div>
              </div>
            </div>
          </div>
        </section>

        <!-- Immersive Why Section -->
        <section class="immersive-why">
          <div class="why-glow"></div>
          <div class="why-container">
            <div class="why-header">
              <span class="why-badge">MISIÓN SALVAR VIDAS</span>
              <h2>¿Por qué esta guía es vital?</h2>
            </div>
            <p class="why-text">
              En una emergencia, cada segundo cuenta. Esta plataforma no es solo
              teoría; es un sistema de entrenamiento diseñado para que <strong
                >tú</strong
              > seas el eslabón que salva la cadena de supervivencia. Basado estrictamente
              en las guías técnicas del <strong>ERC</strong>.
            </p>
            <div class="why-stats">
              <div class="stat-item">
                <span class="stat-num">100%</span>
                <span class="stat-desc">Guías ERC 2025</span>
              </div>
              <div class="stat-item">
                <span class="stat-num">CERCP</span>
                <span class="stat-desc">Aval Oficial</span>
              </div>
            </div>
          </div>
        </section>

        <!-- Learning Path Visualization -->
        <div class="learning-path-section">
          <h2 class="section-title">Estructura del Programa</h2>
          <div class="path-grid">
            <div class="path-step">
              <div class="step-num">01</div>
              <div class="step-card">
                <div class="step-icon">🚨</div>
                <div class="step-info">
                  <h4>Fundamentos SVB</h4>
                  <p>Algoritmos iniciales y reconocimiento de PCR.</p>
                </div>
              </div>
            </div>
            <div class="path-step">
              <div class="step-num">02</div>
              <div class="step-card">
                <div class="step-icon">💗</div>
                <div class="step-info">
                  <h4>RCP Avanzada</h4>
                  <p>Manejo de vía aérea y soporte farmacológico.</p>
                </div>
              </div>
            </div>
            <div class="path-step">
              <div class="step-num">03</div>
              <div class="step-card">
                <div class="step-icon">⚡</div>
                <div class="step-info">
                  <h4>Desfibrilación</h4>
                  <p>Uso experto de monitor-desfibrilador manual y DEA.</p>
                </div>
              </div>
            </div>
            <div class="path-step">
              <div class="step-num">04</div>
              <div class="step-card">
                <div class="step-info">
                  <h4>Marco Legal</h4>
                  <p>Ética y normativa vigente en emergencias.</p>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- CTA Contact Banner -->
        <section class="cta-banner">
          <div class="cta-glow cta-glow-left"></div>
          <div class="cta-glow cta-glow-right"></div>

          <div class="cta-inner">
            <!-- Left: Audience + Contact CTA -->
            <div class="cta-audience">
              <span class="cta-eyebrow">👥 ¿Para quién es esto?</span>
              <h2 class="cta-title">
                Formación presencial
                <span class="cta-title-accent">a medida</span>
                para proteger vidas
              </h2>
              <p class="cta-desc">
                Aurora imparte cursos presenciales de SVB y SVA para sanitarios,
                cuerpos de seguridad, docentes y ciudadanos. Adaptados a cada
                colectivo, con aval oficial CERCP.
              </p>
              <div class="cta-tags">
                <span class="cta-tag">🏥 Sanitarios</span>
                <span class="cta-tag">👮 Policía</span>
                <span class="cta-tag">🚒 Bomberos</span>
                <span class="cta-tag">📚 Docentes</span>
                <span class="cta-tag">👤 Ciudadanos</span>
              </div>
              <a href="mailto:aurora.vizoso@fpusg061.es" class="cta-btn">
                ✉️ Solicitar Formación Presencial
              </a>
            </div>

            <!-- Divider -->
            <div class="cta-divider"></div>

            <!-- Right: Roadmap + Contact -->
            <div class="cta-right">
              <div class="cta-roadmap">
                <span class="cta-eyebrow">📢 Estado de la Plataforma</span>
                <div class="cta-steps">
                  <div class="cta-step done">
                    <span class="cta-step-icon">✓</span>
                    <div>
                      <strong>Módulos Interactivos</strong>
                      <span class="cta-step-badge done-badge">Disponible</span>
                    </div>
                  </div>
                  <div class="cta-step done">
                    <span class="cta-step-icon">✓</span>
                    <div>
                      <strong>Visualización de Algoritmos</strong>
                      <span class="cta-step-badge done-badge">Disponible</span>
                    </div>
                  </div>
                  <div class="cta-step active">
                    <span class="cta-step-icon live">⬤</span>
                    <div>
                      <strong>Simulador Trivial SVA</strong>
                      <span class="cta-step-badge live-badge">En vivo</span>
                    </div>
                  </div>
                </div>
              </div>

              <div class="cta-contact-box">
                <span class="cta-contact-icon">👩‍⚕️</span>
                <div class="cta-contact-info">
                  <strong>Aurora Vizoso García</strong>
                  <span>Enfermera Asistencial · FPUSG 061</span>
                  <span class="cta-contact-note"
                    >Guías ERC 2025 · Aval oficial CERCP</span
                  >
                </div>
              </div>
            </div>
          </div>
        </section>
      </div>
    </div>
  </div>
</StarlightPage>

<style>
  /* Base layout escapes starlight container if needed */
  :global([data-template="splash"] .main-frame) {
    padding-inline: 0 !important;
  }

  :global([data-template="splash"] .sl-container) {
    max-width: none !important;
  }

  /* Full Width Home Styles */
  .full-width-home {
    width: 100%;
    overflow-x: hidden;
  }

  .hero-section {
    position: relative;
    padding: 8.5rem 5% 6rem;
    overflow: visible;
  }

  .hero-bg-glow {
    position: absolute;
    top: 45%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 120vw;
    height: 90vh;
    background:
      radial-gradient(
        circle at 20% 30%,
        rgba(16, 185, 129, 0.12) 0%,
        transparent 40%
      ),
      radial-gradient(
        circle at 80% 70%,
        rgba(6, 182, 212, 0.12) 0%,
        transparent 40%
      );
    z-index: -1;
    pointer-events: none;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 4rem;
    max-width: 1400px;
    margin: 0 auto;
    align-items: center;
  }

  .hero-title {
    margin: 0;
    line-height: 1.1;
  }

  .hero-top {
    font-size: clamp(2.5rem, 5.5vw, 4rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    background: linear-gradient(90deg, #10b981 0%, #06b6d4 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: inline-block;
    margin-bottom: 0.5rem;
  }

  .hero-main {
    font-size: clamp(3.5rem, 8vw, 5.5rem);
    font-weight: 900;
    color: #0f172a;
    letter-spacing: -0.04em;
    display: block;
    line-height: 0.95;
  }

  .hero-divider {
    width: 120px;
    height: 8px;
    background: #10b981;
    margin-top: 2.5rem;
    border-radius: 4px;
  }

  .hero-tagline {
    font-size: 1.35rem;
    color: #475569;
    margin: 3rem 0;
    font-weight: 600;
  }

  .hero-btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background: linear-gradient(90deg, #10b981 0%, #06b6d4 100%);
    color: white;
    padding: 1.2rem 3rem;
    border-radius: 99px;
    font-weight: 700;
    font-size: 1.2rem;
    text-decoration: none;
    box-shadow: 0 20px 40px -10px rgba(16, 185, 129, 0.35);
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .hero-btn-primary:hover {
    transform: translateY(-5px) scale(1.05);
    box-shadow: 0 30px 50px -10px rgba(6, 182, 212, 0.45);
    filter: brightness(1.05);
  }

  .hero-image-wrapper {
    position: relative;
    padding: 20px;
  }

  .hero-image-wrapper::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #10b981 0%, #06b6d4 100%);
    opacity: 0.1;
    filter: blur(60px);
    border-radius: 50%;
    z-index: -1;
  }

  .hero-cpr-img {
    width: 100%;
    border-radius: 48px;
    box-shadow: 0 40px 80px -20px rgba(0, 0, 0, 0.3);
    border: 12px solid white;
    transition: transform 0.5s ease;
  }

  .hero-image-wrapper:hover .hero-cpr-img {
    transform: perspective(1000px) rotateY(-5deg) rotateX(2deg);
  }

  .floater {
    position: absolute;
    z-index: 10;
    pointer-events: none;
  }

  .floater-heart {
    font-size: 3.5rem;
    top: -10px;
    left: -20px;
    animation: floatAnim 4s infinite ease-in-out;
  }
  .floater-ambulance {
    font-size: 3.5rem;
    bottom: 40px;
    right: -20px;
    animation: floatAnim 4.5s infinite ease-in-out reverse;
  }

  .floater-erc {
    top: 15%;
    right: -40px;
    background: #10b981;
    color: white;
    padding: 10px 20px;
    border-radius: 99px;
    font-weight: 900;
    box-shadow: 0 15px 30px rgba(16, 185, 129, 0.4);
    animation: floatAnim 5.5s infinite ease-in-out;
  }

  .floater-saving {
    bottom: 20%;
    left: -40px;
    background: #0ea5e9;
    color: white;
    padding: 10px 20px;
    border-radius: 99px;
    font-weight: 900;
    box-shadow: 0 15px 30px rgba(14, 165, 233, 0.4);
    animation: floatAnim 5s infinite ease-in-out reverse;
  }

  @keyframes floatAnim {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-25px) rotate(5deg);
    }
  }

  .content-sections {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 5% 6rem;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .bento-section {
    background: #f1f5f9;
    margin: 0 -5vw;
    padding: 3rem 5vw;
    border-radius: 48px;
    border: 1px solid #e2e8f0;
  }

  /* Bento Layout: 2-column, left = featured, right = 2x2 grid */
  .bento-layout {
    display: grid !important;
    grid-template-columns: 1fr 1fr !important;
    gap: 1.25rem !important;
    align-items: stretch !important;
  }

  /* The right-side 2x2 sub-grid */
  .bento-subgrid {
    display: grid !important;
    grid-template-columns: 1fr 1fr !important;
    grid-template-rows: 1fr 1fr !important;
    gap: 1.25rem !important;
  }

  /* Featured card fills full height of the left column */
  .bento-card.featured {
    height: 100%;
    background: linear-gradient(135deg, #ffffff 0%, #f1f5f9 100%);
    border: 1px solid rgba(16, 185, 129, 0.15);
    padding: 3rem;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }

  .bento-card.featured .card-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.5rem;
  }

  .bento-card.featured h4 {
    font-size: 2.8rem;
    font-weight: 900;
    line-height: 1.05;
    margin-bottom: 1.25rem;
    color: #0f172a;
  }

  /* Small cards (2x2 right grid) */
  .bento-card.small {
    padding: 1.25rem;
  }

  .bento-card.small .card-icon {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .bento-card.small h4 {
    font-size: 1.1rem;
    margin-bottom: 0.4rem;
  }

  .bento-card.small p {
    font-size: 0.9rem;
    line-height: 1.5;
  }

  .bento-card {
    background: white;
    padding: 1.5rem;
    border-radius: 24px;
    border: 1px solid rgba(0, 0, 0, 0.05);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.02);
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }

  .bento-card:hover {
    transform: translateY(-8px) scale(1.01);
    box-shadow: 0 30px 60px -12px rgba(0, 0, 0, 0.1);
    border-color: #10b981;
  }

  .bento-card.featured {
    background: linear-gradient(135deg, #ffffff 0%, #f1f5f9 100%);
    border: 1px solid rgba(16, 185, 129, 0.15);
    padding: 3rem;
  }

  .bento-card.featured .card-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.5rem;
  }

  .bento-card.featured h4 {
    font-size: 2.8rem;
    font-weight: 900;
    line-height: 1.05;
    margin-bottom: 1.25rem;
    color: #0f172a;
  }

  .card-icon-large {
    font-size: 7rem;
    line-height: 1;
    filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.1));
  }

  .card-list {
    margin-top: 1.5rem;
    padding: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .card-list li {
    font-size: 1.1rem;
    font-weight: 700;
    color: #059669;
  }

  .bento-card.accent {
    background: linear-gradient(135deg, #ecfdf5 0%, #f0fdf4 100%);
    color: #1e293b;
    border: 1px solid #d1fae5;
  }

  .bento-card.accent h4 {
    color: #059669 !important;
  }
  .bento-card.accent p {
    color: #065f46 !important;
  }

  .card-icon {
    font-size: 2rem;
    display: block;
    margin-bottom: 1.25rem;
  }

  .bento-card.image-card {
    grid-column: span 2;
    padding: 0 !important;
    border: none;
    overflow: hidden;
    min-height: 220px;
    max-height: 260px;
    box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
  }

  .bento-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .bento-card.image-card:hover .bento-img {
    transform: scale(1.08);
  }

  .bento-card h4 {
    font-size: 1.6rem;
    font-weight: 800;
    color: #1e293b;
    margin-bottom: 0.75rem;
  }

  .bento-card p {
    font-size: 1.05rem;
    line-height: 1.65;
    color: #475569;
    margin: 0;
  }

  /* Immersive Why Section - Light Version */
  .immersive-why {
    position: relative;
    background: #ffffff;
    border-radius: 48px;
    padding: 6rem 10%;
    color: #1e293b;
    text-align: center;
    overflow: hidden;
    border: 1px solid rgba(0, 0, 0, 0.05);
    box-shadow: 0 40px 100px -20px rgba(0, 0, 0, 0.05);
  }

  .why-glow {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(
      circle at center,
      rgba(16, 185, 129, 0.08) 0%,
      transparent 50%
    );
    pointer-events: none;
  }

  .why-badge {
    background: #f0fdf4;
    color: #10b981;
    padding: 8px 16px;
    border-radius: 99px;
    font-size: 0.8rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    display: inline-block;
    margin-bottom: 2rem;
    border: 1px solid #d1fae5;
  }

  .immersive-why h2 {
    font-size: 3.5rem;
    font-weight: 900;
    margin-bottom: 2rem;
    letter-spacing: -0.02em;
    color: #0f172a;
  }
  .why-text {
    font-size: 1.5rem;
    line-height: 1.6;
    color: #475569;
    max-width: 900px;
    margin: 0 auto 4rem;
  }

  .why-stats {
    display: flex;
    justify-content: center;
    gap: 6rem;
  }

  .stat-num {
    font-size: 3.5rem;
    font-weight: 900;
    color: #10b981;
    display: block;
    letter-spacing: -0.05em;
  }
  .stat-desc {
    font-size: 0.9rem;
    font-weight: 700;
    color: #94a3b8;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Path grid */
  .learning-path-section h2 {
    font-size: 3rem;
    font-weight: 900;
    margin-bottom: 4rem;
    text-align: center;
    color: #1e293b;
  }

  .path-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .path-step {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .step-num {
    font-size: 4rem;
    font-weight: 900;
    color: #e5e7eb;
    line-height: 1;
  }
  .step-card {
    flex: 1;
    background: white;
    padding: 2rem;
    border-radius: 24px;
    display: flex;
    gap: 1.5rem;
    border: 1px solid rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }

  .step-card:hover {
    border-color: #10b981;
  }
  .step-icon {
    font-size: 2.5rem;
  }
  .step-info h4 {
    font-size: 1.25rem;
    font-weight: 800;
    color: #1e293b;
    margin-bottom: 0.25rem;
  }
  .step-info p {
    margin: 0;
    color: #64748b;
    font-size: 0.95rem;
  }

  /* ===== CTA BANNER — Light Version ===== */
  .cta-banner {
    position: relative;
    background: #f8fafc;
    border-radius: 48px;
    padding: 5rem 6%;
    overflow: hidden;
    border: 1px solid rgba(16, 185, 129, 0.15);
    margin: 0 -5vw;
  }

  .cta-glow {
    position: absolute;
    width: 500px;
    height: 500px;
    border-radius: 50%;
    pointer-events: none;
    filter: blur(120px);
    opacity: 0.22;
  }

  .cta-glow-left {
    background: radial-gradient(circle, #10b981, transparent 70%);
    top: -100px;
    left: -100px;
  }

  .cta-glow-right {
    background: radial-gradient(circle, #06b6d4, transparent 70%);
    bottom: -100px;
    right: -100px;
  }

  .cta-inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 4rem;
    align-items: center;
    max-width: 1400px;
    margin: 0 auto;
  }

  .cta-eyebrow {
    display: inline-block;
    font-size: 0.78rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #059669;
    margin-bottom: 1.25rem;
    background: #ecfdf5;
    border: 1px solid #a7f3d0;
    padding: 4px 14px;
    border-radius: 99px;
  }

  .cta-title {
    font-size: clamp(2rem, 3.5vw, 2.8rem);
    font-weight: 900;
    color: #0f172a;
    line-height: 1.15;
    margin: 0 0 1.25rem 0;
  }

  .cta-title-accent {
    background: linear-gradient(90deg, #10b981, #06b6d4);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .cta-desc {
    font-size: 1.05rem;
    color: #475569;
    line-height: 1.7;
    margin: 0 0 2rem 0;
    max-width: 420px;
  }

  .cta-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin-bottom: 2.5rem;
  }

  .cta-tag {
    background: white;
    border: 1px solid #e2e8f0;
    color: #475569;
    padding: 6px 16px;
    border-radius: 99px;
    font-size: 0.88rem;
    font-weight: 600;
    transition: all 0.25s ease;
    cursor: default;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
  }

  .cta-tag:hover {
    background: #ecfdf5;
    border-color: #6ee7b7;
    color: #059669;
  }

  .cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    background: linear-gradient(90deg, #10b981 0%, #06b6d4 100%);
    color: white;
    padding: 1rem 2.5rem;
    border-radius: 99px;
    font-weight: 700;
    font-size: 1.05rem;
    text-decoration: none;
    box-shadow: 0 20px 40px -10px rgba(16, 185, 129, 0.4);
    transition: all 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .cta-btn:hover {
    transform: translateY(-4px) scale(1.04);
    box-shadow: 0 30px 50px -10px rgba(6, 182, 212, 0.5);
    filter: brightness(1.08);
  }

  /* Vertical divider */
  .cta-divider {
    width: 1px;
    height: 320px;
    background: linear-gradient(
      to bottom,
      transparent,
      #cbd5e1 30%,
      #cbd5e1 70%,
      transparent
    );
    flex-shrink: 0;
  }

  /* Right column */
  .cta-right {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  /* Roadmap steps */
  .cta-steps {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1.25rem;
  }

  .cta-step {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.9rem 1.25rem;
    border-radius: 16px;
    background: white;
    border: 1px solid #e2e8f0;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.03);
    transition:
      box-shadow 0.2s ease,
      border-color 0.2s ease;
  }

  .cta-step:hover {
    box-shadow: 0 6px 20px rgba(16, 185, 129, 0.1);
    border-color: #a7f3d0;
  }

  .cta-step-icon {
    font-size: 1rem;
    font-weight: 900;
    color: #10b981;
    width: 28px;
    text-align: center;
    flex-shrink: 0;
  }

  .cta-step-icon.live {
    color: #6366f1;
    font-size: 0.7rem;
    animation: pulseLive 2s infinite ease-in-out;
  }

  @keyframes pulseLive {
    0%,
    100% {
      opacity: 1;
      filter: drop-shadow(0 0 4px #6366f1);
    }
    50% {
      opacity: 0.4;
      filter: none;
    }
  }

  .cta-step div {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .cta-step strong {
    font-size: 0.92rem;
    font-weight: 700;
    color: #1e293b;
  }

  .cta-step-badge {
    font-size: 0.7rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 2px 10px;
    border-radius: 99px;
  }

  .done-badge {
    background: #ecfdf5;
    color: #059669;
    border: 1px solid #a7f3d0;
  }

  .live-badge {
    background: #eef2ff;
    color: #4f46e5;
    border: 1px solid #c7d2fe;
  }

  /* Contact box */
  .cta-contact-box {
    display: flex;
    align-items: flex-start;
    gap: 1.25rem;
    padding: 1.5rem;
    border-radius: 20px;
    background: white;
    border: 1px solid #e2e8f0;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.04);
  }

  .cta-contact-icon {
    font-size: 2.5rem;
    flex-shrink: 0;
  }

  .cta-contact-info {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .cta-contact-info strong {
    font-size: 1rem;
    font-weight: 800;
    color: #0f172a;
  }

  .cta-contact-info span {
    font-size: 0.83rem;
    color: #64748b;
    line-height: 1.5;
  }

  .cta-contact-note {
    color: #94a3b8 !important;
    font-style: italic;
  }

  @media (max-width: 1024px) {
    .bento-grid,
    .path-grid {
      grid-template-columns: 1fr;
    }
    .bento-card.featured {
      grid-column: auto;
      flex-direction: column;
      text-align: center;
    }
    .card-icon-large {
      margin: 0 0 2rem 0;
    }
    .immersive-why h2 {
      font-size: 2.5rem;
    }
    .why-stats {
      gap: 2rem;
      flex-direction: column;
    }
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .hero-actions {
      justify-content: center;
    }
    .hero-divider {
      margin: 2rem auto 0;
    }
    /* CTA Banner responsive */
    .cta-banner {
      padding: 4rem 5%;
      border-radius: 32px;
    }
    .cta-inner {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .cta-divider {
      display: none;
    }
    .cta-title {
      font-size: 2rem;
    }
  }

  @media (max-width: 640px) {
    .cta-banner {
      margin: 0;
      border-radius: 24px;
    }
    .cta-tags {
      gap: 0.5rem;
    }
    .cta-tag {
      font-size: 0.8rem;
      padding: 4px 12px;
    }
  }
</style>
